# ----------------------------------------------------
# 옵시디언 완전 독립 환경을 위한 .gitignore
# ----------------------------------------------------

# OS 기본 파일
.DS_Store
Thumbs.db
*.log

# 옵시디언 휴지통
.trash/

# 옵시디언 캐시 (각 기기에서 생성되도록 함)
.obsidian/cache


# --- 플러그인 완전 비동기화 설정 (핵심) ---

# 커뮤니티 플러그인 폴더 전체 (설치 파일, 개별 설정 모두 포함)
.obsidian/plugins/

# 활성화된 커뮤니티 플러그인 목록
.obsidian/community-plugins.json

# -----------------------------------------


# --- 기기별 독립 설정 (강력 권장) ---

# 현재 열린 파일/창 레이아웃 (PC와 모바일은 다르게 쓰는 것이 편함)
.obsidian/workspace.json
.obsidian/workspaces.json

# 모바일 전용 UI 설정
.obsidian/mobile.json